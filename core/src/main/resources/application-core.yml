spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: admin
    url : jdbc:mysql://localhost:3306/yh_toy_one?serverTimezone=UTC

  jpa:
    hibernate:
      use-new-id-generator-mappings: false
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
